import { sql } from "kysely"; import type { DatabaseContext } from "../../db/database-context.js"; import type { GoodsReceiptInput, PurchaseOrderInput } from "./procurement.types.js";
export async function hasPermission(c:DatabaseContext,p:string):Promise<boolean>{const r=await sql<{allowed:boolean}>`select app.has_business_permission(current_setting('app.business_id',true)::uuid,${p}) as allowed`.execute(c.transaction);return r.rows[0]?.allowed===true;}
export async function createPurchaseOrder(c:DatabaseContext,businessId:string,userId:string,input:PurchaseOrderInput){const order=(await sql<{id:string}>`insert into app.purchase_orders ("businessId","supplierAccountId","storeId","orderNumber","expectedAt","notes","createdBy") values (${businessId}::uuid,${input.supplierAccountId}::uuid,${input.storeId}::uuid,${input.orderNumber},${input.expectedAt??null}::timestamptz,${input.notes??''},${userId}::uuid) returning "id"`.execute(c.transaction)).rows[0]!;for(const line of input.lines){await sql`insert into app.purchase_order_lines ("businessId","purchaseOrderId","inventoryItemId","quantityOrdered","unitCostMinor") values (${businessId}::uuid,${order.id}::uuid,${line.inventoryItemId}::uuid,${line.quantityOrdered},${line.unitCostMinor})`.execute(c.transaction);}return order;}
export async function createReceipt(c:DatabaseContext,businessId:string,userId:string,requestId:string,input:GoodsReceiptInput){const receipt=(await sql<{id:string}>`insert into app.goods_receipts ("businessId","purchaseOrderId","inventoryLocationId","receivedBy","idempotencyKey") values (${businessId}::uuid,${input.purchaseOrderId}::uuid,${input.inventoryLocationId}::uuid,${userId}::uuid,${input.idempotencyKey}) on conflict ("businessId","idempotencyKey") do nothing returning "id"`.execute(c.transaction)).rows[0];if(!receipt)return null;for(const line of input.lines){await sql`insert into app.goods_receipt_lines ("businessId","receiptId","purchaseOrderLineId","inventoryItemId","quantityReceived","quantityRejected","unitCostMinor") values (${businessId}::uuid,${receipt.id}::uuid,${line.purchaseOrderLineId}::uuid,${line.inventoryItemId}::uuid,${line.quantityReceived},${line.quantityRejected??0},${line.unitCostMinor})`.execute(c.transaction);await sql`insert into app.stock_transactions ("businessId","type","reason","actorUserId","requestId","idempotencyKey") values (${businessId}::uuid,'receipt','Goods receipt',${userId}::uuid,${requestId},${`${input.idempotencyKey}:${line.purchaseOrderLineId}`})`.execute(c.transaction);await sql`insert into app.stock_balances ("businessId","inventoryItemId","inventoryLocationId","onHand") values (${businessId}::uuid,${line.inventoryItemId}::uuid,${input.inventoryLocationId}::uuid,${line.quantityReceived}) on conflict ("businessId","inventoryItemId","inventoryLocationId") do update set "onHand"=app.stock_balances."onHand"+excluded."onHand"`.execute(c.transaction); }return receipt;}
