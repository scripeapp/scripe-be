/**
 * Database access for the return authorization and returned item domain belongs here.
 * Repository functions must accept DatabaseContext and must not import the global
 * database.
 */
export {};
