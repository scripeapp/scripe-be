/**
 * Database access for the store, location, sales channel, register, device, shift, and
 * cash domain belongs here. Repository functions must accept DatabaseContext and must
 * not import the global database.
 */
export {};
