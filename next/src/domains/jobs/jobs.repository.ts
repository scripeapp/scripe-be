/**
 * Database access for the background and scheduled work execution domain belongs here.
 * Repository functions must accept DatabaseContext and must not import the global
 * database.
 */
export {};
