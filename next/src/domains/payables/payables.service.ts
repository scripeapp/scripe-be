/**
 * Business workflows and transaction boundaries for the bill, bill line, and bill
 * payment allocation domain belong here.
 */
import type { Database } from "../../db/database.types.js"; import { withDatabaseContext } from "../../db/database-context.js"; import { withIdentity } from "../../db/principal.js"; import * as repo from "./payables.repository.js"; import type { AllocatePaymentInput, CreateBillInput, PayablesOperation } from "./payables.types.js";
export class PayablesService { constructor(private readonly database: Database) {} async createBill(o: PayablesOperation, input: CreateBillInput) { return this.run(o, async c => { if (!await repo.hasPermission(c, "payables.manage")) throw new Error("Missing permission: payables.manage"); return repo.createBill(c, o.businessId, o.userId, input); }); } async allocatePayment(o: PayablesOperation, billId: string, input: AllocatePaymentInput) { return this.run(o, async c => { if (!await repo.hasPermission(c, "payables.manage")) throw new Error("Missing permission: payables.manage"); const result = await repo.allocatePayment(c, o.businessId, o.userId, billId, input); if (!result) throw new Error("Payment exceeds balance, currency differs, bill is missing, or payment reference is already used"); return result; }); } private run<T>(o: PayablesOperation, work: Parameters<typeof withDatabaseContext<T>>[2]): Promise<T> { return withDatabaseContext(this.database, withIdentity(o.requestId, o.userId, o.businessId), work); } }
