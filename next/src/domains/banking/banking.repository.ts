/**
 * Database access for the business account, provider account, identifier, statement,
 * and beneficiary domain belongs here. Repository functions must accept DatabaseContext
 * and must not import the global database.
 */
export {};
