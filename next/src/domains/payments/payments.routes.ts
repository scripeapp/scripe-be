import {Router} from "express";import{getDatabase}from"../../db/database.js";import{requireAuth}from"../../middleware/auth.js";import{PaymentsController}from"./payments.controller.js";import{PaymentsService}from"./payments.service.js";export function createPaymentsRouter(){const r=Router();const c=new PaymentsController(new PaymentsService(getDatabase()));const b='/api/businesses/:businessId/payments';r.use(b,requireAuth);r.post(b,c.record);r.post(`${b}/checkout`,c.initiateCheckout);r.post(`${b}/checkout/:reference/verify`,c.verifyCheckout);return r;}
