/**
 * Database access for the outbound money movement and provider execution domain belongs
 * here. Repository functions must accept DatabaseContext and must not import the global
 * database.
 */
export {};
