/**
 * Database access for the immutable privileged and business action audit domain belongs
 * here. Repository functions must accept DatabaseContext and must not import the global
 * database.
 */
export {};
