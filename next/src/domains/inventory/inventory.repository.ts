/**
 * Database access for the stock custody, movement, reservation, count, transfer, cost,
 * waste, and reorder domain belongs here. Repository functions must accept
 * DatabaseContext and must not import the global database.
 */
export {};
