/**
 * Database access for the provider webhook ingestion and processing domain belongs
 * here. Repository functions must accept DatabaseContext and must not import the global
 * database.
 */
export {};
