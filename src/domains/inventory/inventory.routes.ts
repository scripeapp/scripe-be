import { Router } from "express";
import { getDatabase } from "../../db/database.js";
import { requireAuth } from "../../middleware/auth.js";
import { InventoryController } from "./inventory.controller.js";
import { InventoryService } from "./inventory.service.js";
export function createInventoryRouter(): Router { const router = Router(); const controller = new InventoryController(new InventoryService(getDatabase())); const base = "/api/businesses/:businessId/inventory"; router.use(base, requireAuth); router.get(`${base}/balances`, controller.balances); router.get(`${base}/items`, controller.listItems); router.get(`${base}/items/:itemId`, controller.getItem); router.post(`${base}/items`, controller.createItem); router.get(`${base}/locations`, controller.listLocations); router.post(`${base}/locations`, controller.createLocation); router.get(`${base}/movements`, controller.listMovements); router.post(`${base}/movements`, controller.movement); router.post(`${base}/reservations`, controller.reserve); router.post(`${base}/reservations/:reservationId/release`, controller.release); router.get(`${base}/transfers`, controller.listTransfers); router.post(`${base}/transfers`, controller.createTransfer); router.get(`${base}/transfers/:transferId`, controller.getTransfer); router.post(`${base}/transfers/:transferId/send`, controller.sendTransfer); router.post(`${base}/transfers/:transferId/receive`, controller.receiveTransfer); router.post(`${base}/transfers/:transferId/cancel`, controller.cancelTransfer); router.get(`${base}/counts`, controller.listCounts); router.post(`${base}/counts`, controller.createCount); router.get(`${base}/counts/:countId`, controller.getCount); router.put(`${base}/counts/:countId/lines`, controller.setCountLines); router.post(`${base}/counts/:countId/apply`, controller.applyCount); router.post(`${base}/counts/:countId/cancel`, controller.cancelCount); return router; }
