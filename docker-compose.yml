services:
  postgres:
    image: postgres:16
    container_name: surge-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: surge_dev
    ports:
      - "5432:5432"
    volumes:
      - surge-postgres-data:/var/lib/postgresql/data
      - ./src/db/bootstrap.sql:/docker-entrypoint-initdb.d/10-bootstrap.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d surge_dev"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  surge-postgres-data: